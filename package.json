{
  "name": "segmento-frontend",
  "version": "0.1.0",
  "private": true,
  "description": "Segmento Frontend Monorepo",
  "scripts": {
    "dev:main": "cd apps/main-site && npm run dev",
    "dev:pulse": "cd apps/pulse && npm run dev",
    "build:main": "cd apps/main-site && npm run build",
    "build:pulse": "cd apps/pulse && npm run build"
  },
  "devDependencies": {
    "prettier": "^3.0.0"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}